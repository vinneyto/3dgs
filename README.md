# 3dgs Monorepo

Монорепозиторий с пакетами в `packages/*`.

## Packages

- `@3dgs/examples` → `packages/examples`

## Quick start

```bash
npm install
npm run dev
```

Команды в корне проксируются в `@3dgs/examples`.
